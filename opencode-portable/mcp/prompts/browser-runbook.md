# Browser Runbook Prompt

```text
For browser MCP tasks:
- fetch tab context first
- prefer read-only inspection before clicking
- avoid blocking dialogs
- use console output and screenshots as evidence
- after repeated failures, stop and ask for direction instead of retrying forever
```
